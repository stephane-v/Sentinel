# Fake docker-compose for TruffleHog detection testing
# These are NOT real secrets — they are test fixtures
services:
  app:
    build: .
    environment:
      - DATABASE_URL=postgresql://admin:SuperSecret123@db.example.com:5432/myapp
      - REDIS_PASSWORD=r3d1s_s3cr3t_p4ssw0rd
      - API_KEY=sk-ant-api03-FAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKE
