# Sentinel i18n — English (default)
L_REPORT_TITLE="Sentinel Report — Supply Chain Audit"
L_DATE="Date"
L_TARGET="Target"
L_GRYPE_DB="Grype DB"
L_PROJECTS="Projects"
L_PYTHON="Python"
L_NODEJS="Node.js"

# Verdicts
L_VERDICT="Verdict"
L_VERDICT_CLEAN="CLEAN — No issues detected."
L_VERDICT_INFO="INFO — Minor improvements recommended."
L_VERDICT_ATTENTION="ATTENTION — Suspicious elements require manual verification."
L_VERDICT_CRITICAL="CRITICAL — IOCs or compromised packages detected. Immediate action required."

# Summary table
L_CATEGORY="Category"
L_RESULT="Result"
L_CONFIRMED_IOCS="Confirmed IOCs (files/hashes)"
L_KNOWN_COMPROMISED="Known compromised packages"
L_MALICIOUS_HASHES="Malicious hashes"
L_PIP_AUDIT_VULNS="pip-audit vulnerabilities"
L_NPM_AUDIT_VULNS="npm audit vulnerabilities"
L_GRYPE_VULNS="Grype vulnerabilities"
L_UNICODE_SUSPECT="Suspicious Unicode (source code)"
L_PATTERNS_SUSPECT="Suspicious patterns in code"
L_SECRETS_BUILD="Secrets in build.args"
L_UNPINNED_DEPS="Unpinned dependencies"
L_NO_USER="Dockerfiles without USER"
L_SINGLE_STAGE="Single-stage builds"
L_FALSE_POSITIVES="Filtered false positives"

# Results
L_FOUND="found"
L_CRITICAL="critical"
L_PROJECTS_AFFECTED="project(s) affected"
L_FILES_TO_VERIFY="file(s) to verify"
L_ACROSS="across"
L_FILES="file(s)"
L_PATTERN_S="pattern(s)"
L_ALL_PINNED="all pinned"
L_ALL_WITH_USER="all with USER"
L_ALL_MULTISTAGE="all multi-stage"

# IOCs section
L_IOC_SECTION="IOC Search (Indicators of Compromise)"
L_IOC_CONFIRMED="Confirmed IOCs"
L_SUSPICIOUS_ELEMENTS="Suspicious elements (manual verification required)"
L_IOC_FILE_CONFIRMED="Confirmed IOC file"
L_EXFILTRATION_ENDPOINT="Exfiltration endpoint"
L_SUSPICIOUS_PATTERN="Suspicious pattern"
L_UNICODE_INVISIBLE="Invisible Unicode in source code"
L_NO_IOC="No IOC or suspicious pattern detected."
L_CREATED="created"
L_MODIFIED="modified"

# Python/Node sections
L_NO_VULN_PIP="No pip-audit vulnerability"
L_NO_VULN_NPM="npm audit clean"
L_NO_VULN_GRYPE="No Grype vulnerability"
L_COMPROMISED_PKG="Compromised package"
L_UNPINNED_IN="unpinned dependencies in"
L_UNPINNED_RISK="Risk: a future docker build could pull a compromised version"
L_UNPINNED_ACTION="Action: run pip freeze > requirements.txt inside the container to pin"
L_SHOW_UNPINNED="Show unpinned dependencies"
L_INSTALL_SCRIPTS="Packages with install scripts"
L_GRYPE_VULNS_DETAIL="Grype: %d vulnerabilities"
L_SECOND_OPINION="second opinion, Anchore database"
L_AND_MORE="... and %d more"

# Docker section
L_DOCKER_ANALYSIS="Docker Security Analysis"
L_MULTI_STAGE="Multi-stage build"
L_SINGLE_STAGE_BUILD="Single-stage build"
L_USER_NONROOT="Non-root user defined"
L_NO_USER_ROOT="No USER — container runs as root"
L_SECRETS_IN_ARG="Secrets in ARG/ENV (exposed during build)"
L_SENSITIVE_FILES="Sensitive files copied into build"
L_PIP_NO_LOCK="pip install without lockfile"
L_BUILD_ARGS_DETECTED="build.args detected — manually verify if secrets are exposed"
L_SECRET_EXPOSED="secret exposed during build"
L_NON_SENSITIVE="non-sensitive variable"
L_TO_VERIFY="to verify"

# False positives
L_FP_SECTION="Filtered false positives"
L_FP_INTRO="These files triggered a detection rule but were identified as false positives by Sentinel filters. Listed here for transparency."
L_FP_SHOW="Show filtered files"
L_FP_FILE="File"
L_FP_RULE="Rule"
L_FP_REASON="Filtering reason"
L_FP_IDE="IDE directory"
L_FP_DOC="Documentation file"
L_FP_I18N="i18n/locales file"
L_FP_BINARY="Binary file"
L_FP_IOC_FILE="IOC file"
L_FP_PATTERN="Pattern"
L_FP_UNICODE="Invisible Unicode"
